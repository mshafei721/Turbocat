# System Overview Buffer

## Repository Identity

**Name:** Turbocat
**Type:** Monorepo (Hybrid Web + Backend + Mobile)
**Purpose:** Multi-agent AI coding platform for vibe-coding

## Architecture

### Core Applications

| App | Location | Stack | Purpose |
|-----|----------|-------|---------|
| turbocat-agent | `/turbocat-agent` | Next.js 16, React 19, Tailwind 4, Drizzle ORM | Main web frontend + API |
| backend | `/backend` | Express.js, Prisma, PostgreSQL, Redis/BullMQ | Legacy/secondary backend API |
| expo-poc | `/implementation/phase-0-railway-poc/expo-poc-app` | React Native Expo | Mobile POC |

### Technology Stack

**Frontend (turbocat-agent):**
- Framework: Next.js 16 (App Router)
- UI: React 19, Radix UI, Tailwind CSS 4
- State: Jotai
- Database: Neon PostgreSQL + Drizzle ORM
- Auth: Arctic (GitHub OAuth)
- AI: Vercel AI SDK
- Sandbox: Vercel Sandbox

**Backend (legacy):**
- Framework: Express.js 5
- Database: PostgreSQL + Prisma ORM
- Queue: BullMQ + Redis
- Auth: JWT + bcrypt

### Key Directories

```
/home/user/Turbocat/
├── turbocat-agent/          # Main Next.js application
│   ├── app/                 # Next.js App Router pages
│   ├── components/          # React components
│   ├── lib/                 # Utilities, DB, AI logic
│   ├── __tests__/           # Vitest tests
│   └── skills/              # AI skill definitions
├── backend/                 # Express.js backend
│   ├── src/                 # Source code
│   ├── prisma/              # Prisma schema/migrations
│   └── tests/               # Jest tests
├── agent-os/                # Agent orchestration configs
│   ├── product/             # Product docs (mission, roadmap)
│   ├── specs/               # Feature specifications
│   └── commands/            # Claude Code commands
├── .claude/                 # Claude Code configuration
│   ├── agents/              # Agent definitions
│   ├── commands/            # Slash commands
│   └── skills/              # Skill definitions
└── implementation/          # POC implementations
```

## Entry Points

| Type | Path | Command |
|------|------|---------|
| Web Dev | `/turbocat-agent` | `pnpm dev` |
| Backend Dev | `/backend` | `npm run dev` |
| Web Build | `/turbocat-agent` | `pnpm build` |
| Backend Build | `/backend` | `npm run build` |

## Database Schemas

**turbocat-agent (Drizzle):**
- users, accounts, tasks, connectors, keys

**backend (Prisma):**
- Similar schema, may have divergence

## CI/CD

- GitHub Actions: `.github/workflows/ci.yml`, `.github/workflows/test.yml`
- Husky pre-commit hooks
- Deployment: Vercel (frontend)

## Current State

- Branch: `claude/implement-rlm-system-HHuOV`
- Git Status: Clean
- Last commits focus on: AppLayout fixes, mock data replacement, GitHub connect modal

## Risk Areas (Initial Assessment)

1. **Two backends:** Potential schema/API divergence between Drizzle and Prisma
2. **Environment secrets:** Multiple API keys required
3. **Complex dependencies:** Many integrations (Vercel, GitHub, AI providers)
4. **Mobile POC:** Unclear integration status with main app

---
*Generated by RLM System at session start*
