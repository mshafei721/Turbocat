# Cloudinary Configuration
# Get your credentials from: https://cloudinary.com/console

# Cloudinary Cloud Name (required)
CLOUDINARY_CLOUD_NAME=your-cloud-name

# API Key (required for server-side uploads)
CLOUDINARY_API_KEY=your-api-key

# API Secret (required for server-side uploads)
# NEVER expose this to the client
CLOUDINARY_API_SECRET=your-api-secret

# Client-side accessible cloud name
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your-cloud-name

# Upload Preset for unsigned uploads (recommended for client-side)
# Create this in Cloudinary Dashboard: Settings → Upload → Add upload preset
NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET=your-upload-preset

# Optional: Cloudinary URL (alternative to individual credentials)
# CLOUDINARY_URL=cloudinary://API_KEY:API_SECRET@CLOUD_NAME
